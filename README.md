# Init
```bash
  $> npm init -y
  $> npm i -D parcel
```

# Chatbot
## Init
```bash
  $> npm i openai
  $> npm info vercel
  $> npm i -D vercel@latest

  # delete @types/node-fetch
  $> npm i
  $> npm i node-fetch@latest
```